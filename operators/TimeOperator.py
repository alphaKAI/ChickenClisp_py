from operators.IOperator import IOperator
"""
class TimeOperator(IOperator):
  def call(self, engine, args):
    result = Benchmark.realtime do
      engine.eval args[0]
    end

    puts "Time: #{result}"

    result
  end
end
"""